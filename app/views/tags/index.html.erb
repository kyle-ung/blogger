<h1> All Tags </h1>
<ul>
  <% @tag.each do |tag| %>
    <li>
      <%= link_to tag.name, tag_path(tag) %>
      <% if logged_in? %>
      	<%= link_to '[x]', tag_path(tag), method: :delete, confirm: "Really delete this tag?" %>
   	  <% end %>
   	</li>
  <% end %>
</ul>
