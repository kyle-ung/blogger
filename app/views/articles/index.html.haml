%h1 All Articles

%ul
- @article.each do |article|
    %li{:id=>"article#{article.id}"}=link_to article.title,article_path(article)

= link_to "Create a New Article", new_article_path, :method => :post